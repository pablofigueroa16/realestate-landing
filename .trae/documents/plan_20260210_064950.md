Voy a transformar la sección siguiente (que definiremos como la sección "About" o "Concepto") para que coincida exactamente con el diseño "Modern ALL-IN-ONE Aparthotel" de la imagen y se ajuste a la altura de la pantalla (`100vh`).

### 🎨 Plan de Implementación

1.  **Reestructuración de `page.tsx`**:
    *   Moveré `AboutSection` justo después de `Hero` (ya que este contenido es introductorio) y comentaré temporalmente `FeaturedProperties` si es necesario para mantener el flujo visual de la referencia.

2.  **Rediseño de `AboutSection.tsx`**:
    *   **Layout**: Usaré un contenedor `h-screen` (en escritorio) con un grid de 2 columnas.
    *   **Columna Izquierda (Texto y Servicios)**:
        *   Título: "Modern ALL-IN-ONE Aparthotel".
        *   Descripción: Dos párrafos explicativos sobre el concepto de inversión y formato de resort.
        *   Grid de Servicios: 2 columnas con iconos minimalistas (Restaurantes, Bares, Piscinas, Gym, Coworking, Kids, Spa).
    *   **Columna Derecha (Imagen)**:
        *   Imagen grande con bordes redondeados (`rounded-[2.5rem]`) que ocupe la mayor parte de la altura vertical.
        *   **Tarjeta Flotante**: "INTERIOR CONCEPT: Modern aesthetics with natural materials" posicionada en la esquina inferior izquierda de la imagen.

3.  **Ajuste 100vh**:
    *   Utilizaré `flex-grow` y `h-full` para asegurar que el contenido llene la pantalla sin desbordarse en resoluciones estándar de escritorio.

Usaré iconos de `lucide-react` (`Utensils`, `Wine`, `Waves`, `Dumbbell`, `Laptop`, `Smile`, `Sparkles`) para replicar la lista de servicios.