Voy a crear una nueva sección llamada **`LocationSection`** para replicar el diseño "Prime Location — South Bali" de la imagen, manteniendo la coherencia visual con el estilo "Aura" que hemos estado implementando.

### 🎨 Diseño Visual (Referencia "Location")

1.  **Contenedor**:
    *   Fondo oscuro/imagen con overlay oscuro para crear contraste con las secciones anteriores (que son claras).
    *   Bordes redondeados grandes (`rounded-[2.5rem]`) para mantener el estilo de "tarjeta gigante" del Hero.
    *   Altura ajustada a `100vh` (o cercana, con `min-h`) para que ocupe una pantalla completa.

2.  **Contenido (Izquierda)**:
    *   **Etiqueta**: "PANDAWA AREA" (texto pequeño, uppercase, gris/translúcido).
    *   **Título**: "Prime Location — South Bali" (blanco, grande).
    *   **Descripción**: Texto explicativo sobre la zona de Pandawa y su potencial de inversión.
    *   **Lista de Distancias**:
        *   Estilo de lista con números en círculos (02, 08, 10).
        *   Texto: "Minutes to Pandawa Beach", etc.

3.  **Tarjeta "Market Insight" (Inferior Izquierda)**:
    *   Un bloque con fondo gris oscuro translúcido (`bg-white/5` o similar).
    *   Icono de gráfico de crecimiento (verde).
    *   Texto explicativo sobre el desarrollo del área.

4.  **Fondo**:
    *   Imagen de fondo de un templo balinés o paisaje icónico, oscurecida para que el texto blanco resalte.

### 🛠 Pasos de Implementación

1.  Crear `src/components/LocationSection.tsx`.
2.  Importar los iconos necesarios de `lucide-react` (`TrendingUp`).
3.  Implementar la estructura HTML/Tailwind siguiendo el diseño de referencia.
4.  Añadir `LocationSection` a `src/app/page.tsx` después de `AboutSection`.
5.  Asegurar que sea responsive (stack vertical en móvil, 2 columnas o overlay en desktop).