Voy a crear una nueva sección llamada **`UnitTypesSection`** para replicar el diseño de "Unit Types" de la imagen, reemplazando la sección actual de propiedades para mantener la narrativa visual.

### 🎨 Diseño Visual (Referencia "Unit Types")

1.  **Estructura General**:
    *   Fondo claro (`bg-gray-50` o blanco) para contrastar con la sección oscura anterior ("Location").
    *   Diseño limpio y espacioso, centrado en 3 tarjetas principales.
    *   Ajustado para verse bien en una pantalla completa (`min-h-screen` o `py-20`).

2.  **Encabezado**:
    *   **Izquierda**: Título "Unit Types" y descripción breve.
    *   **Derecha**: Enlace "Get full catalog ->" alineado a la derecha.

3.  **Tarjetas de Unidades (Grid de 3)**:
    *   **Estilo**: Tarjetas blancas con sombra suave (`shadow-xl`), bordes muy redondeados (`rounded-3xl`).
    *   **Imagen**: Parte superior de la tarjeta, también con bordes redondeados.
    *   **Badge**: Etiqueta flotante en la imagen ("from 30 m²").
    *   **Contenido**: Título (ej. "Smart Villas"), descripción corta y un botón "Check Details" de ancho completo con estilo outline.

### 🛠 Pasos de Implementación

1.  Crear `src/components/UnitTypesSection.tsx`.
2.  Implementar el diseño de tarjetas usando Flexbox/Grid.
3.  Usar iconos de `lucide-react` (`ArrowRight`, `ArrowUpRight`).
4.  Actualizar `src/app/page.tsx` para **reemplazar** `FeaturedProperties` con `UnitTypesSection`, siguiendo el flujo lógico (Hero -> Concepto -> Ubicación -> Tipos de Unidad).

Esto completará la transformación de la landing page al estilo "Aura".