Voy a transformar el componente `Footer` para que incluya tanto la sección de "Contact Us" como la barra de pie de página minimalista, replicando exactamente el diseño de la imagen y ajustándolo a `100vh`.

### 🎨 Diseño Visual (Referencia "Contact + Footer")

1.  **Layout General**:
    *   Contenedor de altura completa (`h-screen`) con fondo gris claro.
    *   Distribución Flexbox vertical: Tarjeta de Contacto (ocupa el espacio central) + Barra de Footer (abajo).

2.  **Tarjeta "Contact Us"**:
    *   **Estilo**: Gran tarjeta blanca con bordes muy redondeados (`rounded-[3rem]`).
    *   **Columna Izquierda**:
        *   Título "Contact Us".
        *   Texto descriptivo inspirador.
        *   Datos de contacto (Email y Teléfono) con iconos minimalistas.
    *   **Columna Derecha (Formulario)**:
        *   Campos limpios con etiquetas en mayúsculas pequeñas (FIRST NAME, LAST NAME, EMAIL, PHONE).
        *   Botón negro de ancho completo: "Get project presentation ->".

3.  **Barra Footer Inferior**:
    *   Diseño minimalista fuera de la tarjeta.
    *   Logo "Estetico." a la izquierda.
    *   Enlaces de navegación centrados (About, Unit Types, Video, Contacts).
    *   Copyright a la derecha.

### 🛠 Pasos de Implementación

1.  Actualizar `src/components/Footer.tsx` con el nuevo diseño combinado.
2.  Eliminar `ContactSection` de `src/app/page.tsx` ya que ahora el `Footer` cumple esa función (para evitar duplicidad).
3.  Usar iconos de `lucide-react` (`Mail`, `Phone`, `Building2`).