I will update `src/components/WhyChooseSection.tsx` to ensure the content fits perfectly within `100vh` by compacting the layout:

1.  **Container Height**: Change `h-[89vh]` to `h-screen`.
2.  **Typography & Spacing**:
    *   Reduce title size to `text-3xl md:text-4xl` and margin to `mb-6`.
    *   Reduce description margin to `mb-6`.
    *   Reduce the spacing between list items from `space-y-5` to `space-y-3`.
    *   Reduce the bottom margin of the list to `mb-8`.
3.  **Image Column**: Adjust the image height to `lg:h-[60vh]` to better balance with the compacted text.

This will ensure all content is visible without scrolling on standard screens.