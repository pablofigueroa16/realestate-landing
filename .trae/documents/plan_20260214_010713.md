I will update `src/components/WhyChooseSection.tsx` to replace the static image thumbnail with a video element sourcing `/video_why.mp4`.

1.  **Modify Component**: Replace the existing `img` tag inside the "Left Column" with a `video` tag.
2.  **Video Attributes**:
    *   Set `src` to `/video_why.mp4`.
    *   Enable `autoPlay`, `loop`, `muted`, and `playsInline` to ensure it plays automatically as a background element.
    *   Apply the same CSS classes (`w-full h-full object-cover`) to maintain the design.
3.  **Clean up**: Remove the "Play Button" overlay since the video will be playing automatically.

This will change the visual from a static image with a play button to a live playing video background.