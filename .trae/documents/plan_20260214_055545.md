I will update the Navbar to Spanish and implement single-word navigation links by adding IDs to the corresponding sections.

**1. Update Component IDs**
I will add unique IDs to the main wrapper of each section to enable smooth scrolling navigation:
- `src/components/AboutSection.tsx`: Add `id="nosotros"`
- `src/components/LocationSection.tsx`: Add `id="proyectos"`
- `src/components/UnitTypesSection.tsx`: Add `id="unidades"`
- `src/components/WhyChooseSection.tsx`: Add `id="video"`
- `src/components/Footer.tsx`: Add `id="contacto"`

**2. Update Navbar in `src/components/Hero.tsx`**
I will translate the navigation items and link them to the new IDs:
- "About the Project" → **"Nosotros"** (`#nosotros`)
- "Unit Types" → **"Unidades"** (`#unidades`)
- "Video" → **"Video"** (`#video`)
- "Contacts" → **"Contacto"** (`#contacto`)
- Update the CTA button text: "Agendar consultoria" (already Spanish, will verify consistency).
- Update Hero main text if needed (it seems to be in Spanish already from the `Read` output).

This ensures all links are descriptive, single-word, and functional.